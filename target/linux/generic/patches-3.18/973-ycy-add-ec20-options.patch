Index: linux/drivers/usb/serial/option.c
===================================================================
--- linux.orig/drivers/usb/serial/option.c	2017-05-12 15:49:05.662689001 +0800
+++ linux/drivers/usb/serial/option.c	2017-05-12 16:33:31.090689016 +0800
@@ -625,6 +625,10 @@
 };
 
 static const struct usb_device_id option_ids[] = {
+	{ USB_DEVICE(0x05c6, 0x9090) },
+	{ USB_DEVICE(0x05c6, 0x9003) },
+	{ USB_DEVICE(0x05c6, 0x9215) },
+	{ USB_DEVICE(0x2c7c, 0x0125) },
 	{ USB_DEVICE(OPTION_VENDOR_ID, OPTION_PRODUCT_COLT) },
 	{ USB_DEVICE(OPTION_VENDOR_ID, OPTION_PRODUCT_RICOLA) },
 	{ USB_DEVICE(OPTION_VENDOR_ID, OPTION_PRODUCT_RICOLA_LIGHT) },
@@ -1836,6 +1840,7 @@
 #ifdef CONFIG_PM
 	.suspend           = usb_wwan_suspend,
 	.resume            = usb_wwan_resume,
+	.reset_resume	   = usb_wwan_resume,
 #endif
 };
 
Index: linux/drivers/usb/serial/usb-serial.c
===================================================================
--- linux.orig/drivers/usb/serial/usb-serial.c	2015-10-29 10:49:46.000000000 +0800
+++ linux/drivers/usb/serial/usb-serial.c	2017-05-12 16:38:09.790688999 +0800
@@ -1209,6 +1209,7 @@
 	.disconnect =	usb_serial_disconnect,
 	.suspend =	usb_serial_suspend,
 	.resume =	usb_serial_resume,
+	.reset_resume =	usb_serial_resume,
 	.no_dynamic_id =	1,
 	.supports_autosuspend =	1,
 };
Index: linux/drivers/usb/serial/usb_wwan.c
===================================================================
--- linux.orig/drivers/usb/serial/usb_wwan.c	2017-05-11 16:19:36.996456000 +0800
+++ linux/drivers/usb/serial/usb_wwan.c	2017-05-12 16:38:30.842689077 +0800
@@ -470,7 +470,13 @@
 	usb_fill_bulk_urb(urb, serial->dev,
 			  usb_sndbulkpipe(serial->dev, endpoint) | dir,
 			  buf, len, callback, ctx);
-
+	/*20170511 add by ycy*/
+	if(dir==USB_DIR_OUT){
+	struct usb_device_descriptor *desc=&serial->dev->descriptor;
+	if(desc->idVendor == 0x05C6 && (desc->idProduct == 0x9090 || desc->idProduct == 0x9003 || desc->idProduct == 0x9215))
+		urb->transfer_flags|=URB_ZERO_PACKET;
+}
+	/*20170511 add by ycy*/
 	return urb;
 }
 
